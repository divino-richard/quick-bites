import express, { Request, Response } from 'express';

const merchantRoute = express.Router();

merchantRoute.post('/registration/completion', (req: Request, res: Response) => {

})

export default merchantRoute;
